# dccstatusline — reference configuration
#
# This firoxment variable overrides
# the config path entirely.
#
# Colors, wherever a *_fg or *_bg key appears:
lse derives
# it from used/ceiling. Percent signs are part of the token.
[context]
format  = {label} {used}/{ceiling} {pct}
label   = ctx
fg      = bright_black
used_fg = cyan
pct_fg  = bright_cyan

�
# Claude Code reports a short and a long rate-limit window (5-hour and
# 7-day today). {window} renders the current tag ("5h"/"7d"), so these
# sections survive any future window change without c��──
# Cisables.
thousands = ,

# ── cwd ─────────────────────────�e, or an 8-hex short SHishes outside a repository.
[git]
format = {branch}
fg     = bright_green

# ── midel ──────────────────────────────────────────────────────────────
# Tokens: {rt} {id}
# {ver} is derived from the model id (claude-fable-5 → "5# when {name} already ends with it — "Fable 5", never "Fable 5 5".
[model]
format = {name} {ver} ·{effort}
fg     = bright_magenta
effort_fg = bright_bt_black
pct_fg = bright_yellow

[plan_long]
format = {window} {pctYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYY}
fg     = bright_black
pct_fg = yellow
# To append the local reset time:
# format = {window} {pct} ↻{resets}
