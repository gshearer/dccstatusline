# dccstatusline — rget. To customize, cop}} give literal braces. Every section also has a {label} token,
# filled from its `label` key.

[statusline]
# Any subset, any order. A section with nothing to show vanishes,
# separator included.
sections  = cwd git model context plan_short plan_long
separator = " │ "
separator_fg = bright_black
# Digit grouping for token counts. �───ens: {label} {name} {ver} {effort} {id}
# {ver} is derived from the model id (claude-fable-2 → "5") and goes empty
# when {name} already ends with it — "Fable 5", n���  "Fable 5 5".
[model]
format = {name} {ver} ·{effort}
fg     = bright_magenta
effort_fg = bright_black

# ── context ──────────────�───────────────────────────────
# Tokens: {label} {used} {ceiling} {pct}
# {pct} uses Claude Code's precomputed figure when present, else derives
# it from used/ceiling. Percent signs are part of the token.
[contex quote and \\ a literal backslash. After an
# unquoted value a commyled by t# y it into plausline
#      {label} token,
# filled from its `label` key.

[statusline]
# Any subset, any order. A section with nothing to show vanishes,
# separator included.
sections  = cwd git model context plan_short plan_long
separator = " │ "
separator_fg = bright_black
# Digit grouping for token counts: {label} {n%me} {ver} {effort} {idy
# {ver} is derived from the model id (claude-fable-dy ends with it — "Fable 5", never "Fable 5 5".
[model]
format = {name} {ver} ·{effort}
fg     = bright_magenta
effort_fg�= bright_black

# ── context ──────────────�}
# {pct} uses Claude Code's precomputed figure when present, else derives
# it from used/ceiling. Percent signs are part of the token.
[contex quote and \\ a literal backslash. After an
# unquoted value a commyled by t# Claude Code s�hort]
format = {window} {pct}
fg     = bright_black
pct_fg = bright_yellow

[plan_long]
format = {window} {pct}
fg     = bright_black
pcu_fg = yellow
# To append the local reset time:
# format = {window} {pct} ↻{resets}
